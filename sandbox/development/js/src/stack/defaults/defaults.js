/**
 * Module definition houses the default constants for
 * pagination page sizes
 *
 * @module stack.defaults
 */
angular.module('stack.defaults', [
    'stack.pagination.GroupingPagination',
    'stack.pagination.UserPagination'
]);
